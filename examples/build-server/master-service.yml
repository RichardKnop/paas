---
  apiVersion: v1
  kind: Service
  metadata:
    name: build-server-master
    labels:
      name: build-server-master
  spec:
    ports:
      # the port that this service should serve on
    - port: 80
    selector:
      name: build-server-master
